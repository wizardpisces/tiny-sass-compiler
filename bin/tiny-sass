#!/usr/bin/env node

const run = require('../index.js');

const [node_env, bin_path, ...args] = process.argv;

/**
 * Todos:
 * optimize
 * Initialize CLI
 */

// var cli = meow({
//     pkg: pkg,
//     version: pkg.version,
//     help: `
//                 Usage:
//                     tiny-sass sourceFile targetFile
//             `
// });

// console.log(`this is the arguments: ${node_env},${bin_path},${args}`)

run(args[0], args[1],{generateAstFile:true})